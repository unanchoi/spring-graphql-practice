type Query {
    products: [Product]
}

type Mutation {
    addProduct(title: String, imageUrl: String, price: Int, location: String, content: String): Product
}

type Product {
    id: ID!
    title: String
    imageUrl: String
    price: Int
    location: String
    uploadDate: String
}